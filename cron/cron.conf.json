{
  "webPort": 9990,
  "jobs": [
    {
      "command": "bash",
      "args": [
        "-c",
        "deno run --allow-read --allow-write --allow-run --allow-env gallery-update.ts -p 12 /downloads/gallery-dl && deno run --allow-read --allow-write --allow-run --allow-env --allow-ffi grid.ts /downloads/gallery-dl"
      ],
      "schedule": "0 5 * * *"
    },
    {
      "command": "bash",
      "args": ["-c", "echo \"Testing cron - $(date)\""],
      "schedule": "* * * * *"
    }
  ]
}
